{
    "gcloud_installation_path": "<path to gcloud/bin installed locally>",
    "env": {
        "GOOGLE_AUTHENTICATION_CREDENTIALS": "path to google-auth json file whose account details to be used via gcloud"
    }
}
